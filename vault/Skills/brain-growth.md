---
type: skill
---

# Brain growth

1. `oss brain_pack` — ingest RAG/memory/LLM READMEs from GitHub.
2. `extract url` / `extract file` — turn the web and PDFs into vault sources.
3. Vault RAG + knowledge graph load every turn.
4. Router names the smallest specialist set — spawn only when parallel value is real.
5. Prefer local graph/RAG before another model call.
