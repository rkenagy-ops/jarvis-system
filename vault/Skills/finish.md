---
type: skill
name: finish
---

# Finished OS (5.0)

WordPress is the live out-door. X tweets only with user OAuth (not bearer).

- **BACKUP** zips vault + SQLite to `workspace/backups/` (never `.env`)
- Briefing writes an eval note
- FINISH rail is the checklist

You still add: WP app password, optional Alpaca, optional X user tokens.
