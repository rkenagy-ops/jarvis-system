---
type: skill
name: finish
---

# Finished OS (5.1)

WordPress is the live out-door. X tweets only with user OAuth (not bearer).
Microsoft Graph is the calendar + outbound mail.

- **BACKUP** zips vault + SQLite to `workspace/backups/` (never `.env`); weekly job
- Briefing writes an eval note and speaks
- **MICROSOFT** device-login for Outlook calendar + mail
- Local vault embeddings via Ollama `nomic-embed-text`
- FINISH rail is the checklist

You still add: Azure `MS_CLIENT_ID` + device login, WP app password, optional Alpaca, optional X user tokens.
