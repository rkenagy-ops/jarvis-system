---
type: skill
---

# Setup

1. `.\setup.ps1` then `.\start.ps1`
2. HUD: http://127.0.0.1:8787
3. Obsidian: File → Open folder as vault → `vault/`
4. Credits: https://console.x.ai
5. GitHub: `gh auth login` (already done as rkenagy-ops)
6. Docker extras (optional): n8n, Stirling, whisper — not required
