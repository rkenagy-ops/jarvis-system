fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.28.0
websockets>=14.0
python-dotenv>=1.0.1
python-multipart>=0.0.18
pydantic>=2.10.0
pytest>=8.3.0
pypdf>=5.0.0
youtube-transcript-api>=1.0.0
trafilatura>=2.0.0
feedparser>=6.0.11
beautifulsoup4>=4.12.0
yfinance>=0.2.40
pandas>=2.2.0
numpy>=2.0.0
# ib_insync is archived upstream; ib_async is the maintained fork.
# app/ibkr.py prefers ib_async and falls back to ib_insync if only that is present.
# Upper bound is deliberate: ib_async reserves interface-breaking changes for major
# versions, so 3.x could break our call sites. Verified compatible against 2.1.0.
ib_async>=2.1,<3
markitdown>=0.1.0

# --- optional but used by shipped features -----------------------------------
# events.py: real filesystem events. Without it the vault watcher silently
# degrades to 30s polling, which still works but is not event-driven.
watchdog>=4.0.0
# desktop.py: CPU/RAM/battery in the HUD. Falls back to a Windows-only shim.
psutil>=5.9.0
